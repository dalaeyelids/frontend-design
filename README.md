# frontend-design

这是一个最小可运行的 Vite + React + Tailwind 项目，用来承载根目录的 `frontend.tsx` 页面组件。

## 运行

1. 安装依赖

```bash
npm install
```

2. 启动开发服务器

```bash
npm run dev
```

默认会在终端输出本地访问地址（通常是 `http://localhost:5173`）。

